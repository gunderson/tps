.user-pattern.component
	.remove-button
	.title
		| User Pattern
	.inputs
		.threshold-input.input(data-connection-id="#{thresholdInputId}")
			.port
			| Threshold
	.outputs
		.output.output-a(data-connection-id="#{outputId}")
			.port
			| Output A
	.output-display
		svg.waveform
		.measure-bars
	.waveform-display
		//shows waveform for length of this pattern
		svg.waveform
		.measure-bars